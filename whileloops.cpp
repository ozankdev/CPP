#include <iostream>

// While loop if'e benzer sadece bir koşul sağlanana dek başka bir koşul koyulur!

int main() {

    std::string name;

    // while(name.empty()) {
    //     std::cout << "Enter Your Name! ";
    //     std::getline(std::cin, name);
    // }

    // std::cout << "Hello " << name;

    while (1==0) {
        std::cout << "HELP! I'M STUCK IN AN INFINITE LOOP!";
    }

    return 0;
}